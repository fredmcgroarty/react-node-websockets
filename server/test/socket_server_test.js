const chai = require('chai');
const PORT = process.env.PORT || 1234
const expect = chai.expect
const http = require('http')

describe("Server", () => {
});
describe('a chat app can be mocked', function(t) {
});
